#include <iostream>

using namespace std;


int binary_search(){

    //creating some dummy list of nums in ascending order:
    int arr[50];
    int length = arr.size(); 

    for(int i = 0; i < length; i++){
        cout << "loading num into array" << endl;
        cin >> arr[i];
    }

    int start = 0;
    int end = length - 1;

    while 

}